server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Nick-Saim-MJ/DAD-2024.git
          search-paths: config-data
          #username:
          #password:
          default-label: registry-server
  security:
    user:
      name: root
      password: s3cr3t
